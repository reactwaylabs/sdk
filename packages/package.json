{
    "name": "sdk",
    "private": true,
    "scripts": {
        "build": "yarn workspaces foreach --no-private run build",
        "test": "yarn workspaces foreach --no-private run test "
    },
    "workspaces": [
        "webpack-plugins/*"
    ]
}
